@import 'list';

.search-tips {
	margin-top: 25px;
	line-height: 32px;
	span em {
		font-size: 20px;
		margin-right: 5px;
	}
	a {
		color: theme_color;
		padding: 0 5px;
	}
}

.search-pack {
	icons-item();
}

.more-word {
	text-align: center;
	margin-bottom: 30px;
	a {
		padding: 3px 10px;
		color: #fff;
		background-color: theme_color;
		margin: 0 5px;
		border-radius: 3px;
	}
}